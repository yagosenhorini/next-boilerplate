import { Meta, StoryObj } from '@storybook/react';
import Input from '.';

export default {
  title: 'Input',
  component: Input,
  tags: ['autodocs'],
} as Meta;

export const Default: StoryObj = {};
