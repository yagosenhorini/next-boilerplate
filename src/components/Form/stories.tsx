import { Meta, StoryObj } from '@storybook/react';
import Form from '.';

export default {
  title: 'Form',
  component: Form,
  tags: ['autodocs'],
} as Meta;

export const Default: StoryObj = {};
